{
  "name": "my-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.24.0",
    "bootstrap": "^5.1.3",
    "cross-env": "^7.0.3",
    "eslint": "^8.5.0",
    "json-server": "^0.17.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-router-dom": "^6.0.2",
    "react-scripts": "4.0.3",
    "react-stars": "^2.2.5",
    "react-tabs": "^3.2.3",
    "react-tooltip": "^4.2.21"
  },
  "scripts": {
    "start": "cross-env REACT_APP_ENV=development && npm run lint && react-scripts start",
    "json-server": "json-server --watch db.json --port 3003",
    "build": "cross-env REACT_APP_ENV=production react-scripts build",
    "lint": "./node_modules/.bin/eslint --ext=js --ext=jsx ."
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
